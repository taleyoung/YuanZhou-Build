{
  "name": "demo-yuanzhou-server",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@types/jest": "^25.2.1",
    "@types/mocha": "^7.0.2",
    "egg-cors": "^2.2.3",
    "egg-mysql": "^3.0.0",
    "egg-swagger-doc": "^2.3.2",
    "midway": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^6.0.0",
    "midway-bin": "1",
    "midway-demo-lib": "1"
  },
  "engines": {
    "node": ">=10.16.0"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=local midway-bin dev --ts",
    "debug": "cross-env NODE_ENV=local midway-bin debug --ts",
    "test": "midway-bin test --ts",
    "cov": "midway-bin cov --ts",
    "ci": "midway-bin cov --ts",
    "build": "midway-bin build -c"
  }
}
